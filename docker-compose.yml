version: "3.8"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - questionnaire_data:/root/Documents/questionnaire-manager
    environment:
      - FLASK_ENV=production

volumes:
  questionnaire_data:
